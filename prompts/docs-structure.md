---
description: Create docs/ directory skeleton
outputs: ["docs/README.md"]
---

# Task: Create docs/ Directory Structure

**Output:** `{{PROJECT_DIR}}/docs/` (directory skeleton)

You are creating the `docs/` directory skeleton and the index / placeholder files
that other tasks will later populate.

## Your Tasks

Create these tasks now with TaskCreate:

1. "Detect whether this project is an app and/or has frontend code"
2. "Create docs/ and all subdirectories (with .gitkeep where needed)"
3. "Write docs/README.md (the directory index)"
4. "Write docs/design-docs/index.md (stub index for design decisions)"
5. "Write docs/product-specs/index.md (stub index for product specs)"
6. "Write docs/references/llms.txt (stub for external API references)"
7. "Write docs/generated/README.md (explains the auto-generated sub-directory)"
8. "Verify every directory listed in the target structure exists"

Use TaskUpdate to mark each complete. Use TaskList before finishing.

## Context

Detect whether this project is an app (has `scripts.start`/`scripts.dev` in
`package.json`, or `main.py`/`main.go`/`Dockerfile`) and whether it has frontend
code (`.tsx`/`.jsx`/`.vue`/`.svelte` files). Use those decisions below.

## Required Structure

Only `CLAUDE.md` and `ARCHITECTURE.md` live at the project root. Every other
uppercase knowledge-base file lives inside `docs/`. This mirrors the
harness-engineering layout from the OpenAI article.

```
<project root>
├── CLAUDE.md             ← generated by `claude-md` task
├── ARCHITECTURE.md       ← generated by `architecture-md` task
└── docs/
    ├── README.md                   ← docs index (create this)
    ├── WORKTREE.md                 ← generated by `worktree` task (the most important file)
    ├── GIT_WORKFLOW.md             ← generated by `git-workflow` task
    ├── INFRASTRUCTURE.md           ← generated by `infrastructure-md` task
    ├── QUALITY_SCORE.md            ← generated by `quality-score` task
    ├── PLANS.md                    ← generated by `plans` task
    ├── PRODUCT_SENSE.md            ← generated by `product-sense` task
    ├── RELIABILITY.md              ← generated by `reliability` task
    ├── SECURITY.md                 ← generated by `security` task
    ├── design-docs/
    │   ├── index.md                ← stub index (create this)
    │   └── core-beliefs.md         ← generated by `core-beliefs` task
    ├── exec-plans/
    │   ├── active/
    │   │   └── .gitkeep
    │   ├── completed/
    │   │   └── .gitkeep
    │   └── tech-debt-tracker.md    ← generated by `tech-debt-tracker` task
    ├── generated/
    │   └── README.md               ← explains this dir (create this)
    ├── product-specs/
    │   └── index.md                ← stub index (create this)
    └── references/
        └── llms.txt                ← stub (create this)
```

Only create the `docs/` subdirectories and the index/stub files listed above.
The uppercase knowledge-base files are written by their own dedicated tasks —
do NOT write content into `docs/QUALITY_SCORE.md`, `docs/PLANS.md`, etc.

If this is a frontend project, also create:
- `docs/DESIGN.md` placeholder (the `design` task will fill it in)
- `docs/FRONTEND.md` placeholder (the `frontend` task will fill it in)

If this is an app, also create:
- `docs/OBSERVABILITY.md` placeholder (the `observability` task will fill it in)

A "placeholder" is a one-line file: `# <Title> (placeholder — will be filled by the <name> task)`.

## docs/README.md Content

Write exactly this (adjust the frontend/app rows based on detection):

```markdown
# Docs

This directory is the system of record for this project's living documentation.

| Document | Purpose |
|----------|---------|
| [WORKTREE.md](WORKTREE.md) | **Read first.** Worktree-first local dev + per-worktree service isolation |
| [GIT_WORKFLOW.md](GIT_WORKFLOW.md) | Branching, commits, PR process |
| [INFRASTRUCTURE.md](INFRASTRUCTURE.md) | Services, CI/CD, databases |
| [QUALITY_SCORE.md](QUALITY_SCORE.md) | Quality grades per domain |
| [PLANS.md](PLANS.md) | Active work and roadmap |
| [PRODUCT_SENSE.md](PRODUCT_SENSE.md) | Domain terminology and UX conventions |
| [RELIABILITY.md](RELIABILITY.md) | SLOs, failure modes, on-call |
| [SECURITY.md](SECURITY.md) | Threat model, auth, secrets |
| [OBSERVABILITY.md](OBSERVABILITY.md) | Logging, metrics, tracing (if an app) |
| [DESIGN.md](DESIGN.md) | Design system and component patterns (if frontend) |
| [FRONTEND.md](FRONTEND.md) | Frontend architecture (if frontend) |
| [design-docs/](design-docs/) | Architecture decisions |
| [exec-plans/](exec-plans/) | Execution plans and tech debt |
| [product-specs/](product-specs/) | Product specifications |
| [references/](references/) | External API references (llms.txt files) |
| [generated/](generated/) | Auto-generated docs (db schema, API routes, etc.) |

The docs in this directory and at the project root are kept fresh by the
**claude-harness gardener** on a cron schedule — it scans for references that
no longer match the live code and commits fix-up edits in place.

Generated by claude-harness.
```

## docs/design-docs/index.md Content

```markdown
# Design Docs

Architecture decisions and design explorations. Each doc captures a problem,
the options considered, and the decision made, with a verification status.

## Core

- [core-beliefs.md](core-beliefs.md) — agent-first operating principles

## Decision log

_No decision docs yet. Add one per significant architectural choice._
```

## docs/product-specs/index.md Content

```markdown
# Product Specs

Product specifications — one file per feature or product surface. Each spec
captures the user problem, acceptance criteria, and UX notes.

## Specs

_No specs yet. Add one per feature._
```

## docs/references/llms.txt Content

```markdown
# External API References

List external APIs, SDKs, or services that Claude should know about when
working in this repo. One `*-llms.txt` per reference — download the official
llms.txt and drop it here, then link from this file.

## References

_None yet. Add `<name>-llms.txt` files alongside this one._
```

## docs/generated/README.md Content

```markdown
# Generated Docs

Auto-generated documentation lives here. These files are **not** hand-edited —
they are produced by scripts (DB schema dumpers, API route extractors, etc.)
and regenerated as the code changes.

## Typical contents

- `db-schema.md` — schema extracted from migrations or the live database
- `api-routes.md` — route list extracted from the framework

_No generated docs yet. Wire up generators as the project grows._
```

## Rules

- Create `.gitkeep` in directories that will remain empty so git tracks them.
- Do NOT write content into files owned by other tasks (`QUALITY_SCORE.md`,
  `PLANS.md`, `core-beliefs.md`, `tech-debt-tracker.md`, etc.). Only write
  placeholders for the ones marked "if frontend" / "if an app".
- Check before creating — skip files/dirs that already exist.
- All links in `docs/README.md` and `docs/design-docs/index.md` are relative
  to the file's own directory (no `docs/` prefix in those links, since they're
  already inside `docs/`).
